{
  "name": "pulsefall",
  "version": "1.0.0",
  "description": "A rhythm game where every track is synthesized live in your browser. No audio files — the beatmap IS the score.",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
